# Clé API Syncthing locale à ce nœud — NE PAS COMMITER les modifications
# Régénérer après docker compose up : bash scripts/setup-syncthing-key.sh
# proxy_set_header X-API-Key "CLE_GENEREE_PAR_LE_SCRIPT";
